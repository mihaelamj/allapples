import XCTest
@testable import AllApples

final class AllApplesTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        //XCTAssertEqual(AllApples().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
